import styled from 'styled-components';

const StyledVid = styled.div`
    flex:4;
    overflow-Y:auto;
    overflow-X:hidden;
    height:100%;
    &::-webkit-scrollbar{display:none} 
`;

export default StyledVid;
